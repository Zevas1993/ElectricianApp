// Simplified source set fix
android.sourceSets {
    main.java.srcDirs += 'src/main/java'
    main.java.includes = ['**/*.java', '**/*.kt']
}

// Skip Kotlin-specific configurations to avoid org reference issues
// We've moved these to the main build.gradle files
